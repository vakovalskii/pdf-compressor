# PDF Compressor - Локальный сервис сжатия PDF

Веб-приложение для локального сжатия PDF файлов с выбором качества. Все файлы обрабатываются на вашем компьютере без загрузки в облако.

## 🌐 Попробовать онлайн

**GitHub Pages версия (клиентская):** https://vakovalskii.github.io/pdf-compressor/

⚠️ **Ограничения онлайн версии:**
- Максимум 20 MB
- Сжатие 20-40% (vs 70-90% локально)
- Медленная обработка
- Не работает со сложными PDF

💡 **Для файлов > 20 MB или лучшего сжатия установите локальную версию** ↓

## Возможности

### 🗜️ Сжатие PDF (серверная версия)
- 🔒 **Полная конфиденциальность** - все файлы обрабатываются локально
- 📊 **3 уровня качества** - низкое (72 DPI), среднее (150 DPI), высокое (300 DPI)
- ⚡ **Быстрая обработка** - использует Ghostscript для эффективного сжатия (70-90%)
- 🗑️ **Автоматическая очистка** - временные файлы удаляются после обработки

### ✂️ PDF Редактор (браузерная версия)
- 📄 **Просмотр всех страниц** - визуальные превью каждой страницы
- ✂️ **Удаление страниц** - убирайте ненужные страницы одним кликом
- 🔄 **Изменение порядка** - перетаскивайте страницы для изменения порядка
- 💾 **Сохранение** - создание нового PDF с выбранными страницами
- 🔒 **Приватность** - всё работает в браузере без отправки на сервер

### 🎨 Интерфейс
- 📱 **Адаптивный дизайн** - работает на всех устройствах
- 🎨 **Современный UI** - красивый и удобный интерфейс

## Требования

- Python 3.8+
- Ghostscript (для сжатия PDF)

## Установка

### 1. Установите Ghostscript

**macOS:**
```bash
brew install ghostscript
```

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install ghostscript
```

**Windows:**
Скачайте и установите с [официального сайта](https://www.ghostscript.com/download/gsdnld.html)

### 2. Установите Python зависимости

```bash
pip install -r requirements.txt
```

## Запуск

```bash
python main.py
```

Или с помощью uvicorn:

```bash
uvicorn main:app --host 0.0.0.0 --port 8000
```

Приложение будет доступно по адресу: http://localhost:8000

## Три инструмента в одном

### 1️⃣ Сжатие PDF (серверная версия) - рекомендуется ⭐
**URL:** http://localhost:8000

Использует Ghostscript для профессионального сжатия (70-90% уменьшения размера).
Файлы обрабатываются локальным Python процессом.

### 2️⃣ PDF Редактор (браузерная версия) - полезно! ✂️
**URL:** http://localhost:8000/pdf-editor.html

Удаление и перестановка страниц PDF прямо в браузере.
- Визуальный просмотр всех страниц
- Drag & Drop для изменения порядка
- Удаление ненужных страниц
- Сохранение нового PDF

### 3️⃣ Сжатие в браузере (клиентская версия)
**URL:** http://localhost:8000/index-client-side.html

⚠️ Экспериментальная версия. Сжатие слабое (0-40%), работает медленно.
Используйте для демонстрации или если нельзя отправлять файлы на сервер.

**Подробнее:** см. [CLIENT-SIDE.md](CLIENT-SIDE.md)

## Использование

1. Откройте браузер и перейдите на http://localhost:8000
2. Перетащите PDF файл в область загрузки или нажмите для выбора файла
3. Выберите уровень качества сжатия:
   - **Низкое качество (72 DPI)** - максимальное сжатие, подходит для просмотра на экране
   - **Среднее качество (150 DPI)** - оптимальный баланс размера и качества
   - **Высокое качество (300 DPI)** - подходит для печати, минимальная потеря качества
4. Нажмите "Сжать PDF"
5. Сжатый файл автоматически скачается

## Структура проекта

```
pdf-compressor/
├── main.py              # FastAPI backend
├── requirements.txt     # Python зависимости
├── README.md           # Документация
├── static/             # Frontend файлы
│   ├── index.html      # HTML страница
│   ├── styles.css      # Стили
│   └── script.js       # JavaScript логика
└── temp/               # Временные файлы (создается автоматически)
```

## Технологии

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **Ghostscript** - мощный инструмент для обработки PDF

### Frontend
- **HTML5** - структура страницы
- **CSS3** - современный дизайн с градиентами и анимациями
- **Vanilla JavaScript** - без зависимостей, чистый JS

## API Endpoints

### POST /compress
Сжимает загруженный PDF файл

**Параметры:**
- `file` (file) - PDF файл для сжатия
- `quality` (string) - уровень качества: "low", "medium", "high"

**Ответ:**
- Сжатый PDF файл

### GET /health
Проверка работоспособности сервиса

**Ответ:**
```json
{
  "status": "ok",
  "ghostscript_available": true
}
```

## Безопасность

- Все файлы обрабатываются локально на вашем компьютере
- Временные файлы автоматически удаляются после обработки
- Максимальный размер файла: 100 MB
- Поддерживаются только PDF файлы

## Устранение неполадок

### Ошибка "Ghostscript не установлен"
Убедитесь, что Ghostscript установлен и доступен в PATH:
```bash
gs --version
```

### Ошибка при сжатии
- Проверьте, что файл является валидным PDF
- Убедитесь, что размер файла не превышает 100 MB
- Проверьте логи сервера для подробной информации

## Лицензия

MIT License - свободное использование для личных и коммерческих целей.

## Автор

Создано для локального сжатия PDF файлов без использования облачных сервисов.

